numpy>=1.24
scipy>=1.10
sounddevice>=0.4.6
PySide6-Essentials>=6.11
pyqtgraph>=0.13
# For the phone / browser pickup:
#   cryptography - serve HTTPS (phone browsers block the mic on plain HTTP)
#   aiortc       - the WebRTC transport option (PCM over WebSocket needs neither,
#                  but WebRTC is steadier on a marginal Wi-Fi link)
# The rest of the app works without both.
cryptography>=42
aiortc>=1.6

# QR code for the Phone Portal URL. Pure Python -- no image library needed.
qrcode>=7.4
